# briefing for 2026-09-25 (Fri) — from eod 2026-09-24

- config row must still be 12 (v18); nothing promoted or pending.
- known plumbing issue: live VPIN (IEX stream + SIP warmup) ≠ replay VPIN (SIP) in 09:30–10:00; yesterday AMZN/MSFT entered 5 and 14 bars off the replay. note near-miss `raw_vpin` values for 09:30–09:45 per ticker so tonight's report can compare against the replay; don't alert on it.
- confirm `cross_1m` has real values by 09:32 ET (09:30 `none` is normal).
- the 16:10 ET "paper-trader NOT active" critical is a known false alarm (watchdog window vs 16:10 stop) — ignore it until the watchdog is fixed.
- human: `export-day.timer` is enabled but not started (`systemctl --user start export-day.timer`); approve/reject RQ-3.
- context: 5 trades in 9 sessions (0.56/day vs 0.4), realized slip vs engine +0.2 bps mean (n=5, no net cost), P&L since 09-14 −$11.58.
